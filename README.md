ajimi-training
============================================================

セットアップ
------------------------------------------------------------

1.bundle install
    ```
    bundle install --path vendor/bundle
    ```
